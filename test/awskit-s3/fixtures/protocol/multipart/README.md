# Multipart Fixtures

Constructed from Awskit's typed multipart checksum and part values plus the SDK
request encoder. They lock initiation headers and completion XML without
requiring network access.
